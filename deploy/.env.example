# 镜像配置
IMAGE_NAME=ghcr.io/forzenfox/trmd
IMAGE_TAG=latest

# 端口映射（宿主机端口:容器端口，默认 8000）
HOST_PORT=8000

# WebUI 访问地址（部署到服务器时改为实际域名，如 https://trmd.example.com）
# 留空则使用 config.yaml 中的 webui.base_url
TRMD_WEBUI_URL=

# 时区
TZ=Asia/Shanghai

# 容器名
CONTAINER_NAME=trmd